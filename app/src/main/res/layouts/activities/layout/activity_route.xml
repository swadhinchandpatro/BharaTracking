<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:fitsSystemWindows="true"
    tools:context=".activities.RouteActivity">

    <fragment
        android:id="@+id/map_with_route"
        android:name="com.google.android.gms.maps.MapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/actionbar_with_datetime_size"/>

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/gradient_one"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"/>

    </android.support.design.widget.AppBarLayout>

    <LinearLayout
        android:id="@+id/pick_datetime"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="?android:attr/actionBarSize"
        android:background="#88c9ec">

        <Button
            style="?android:attr/buttonStyleSmall"
            android:background="@drawable/ripple_effect"
            android:id="@+id/start_date"
            android:layout_width="80dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@string/start_date_text"
            android:textColor="@color/black"/>
        <Button
            style="?android:attr/buttonStyleSmall"
            android:background="@drawable/ripple_effect"
            android:id="@+id/end_date"
            android:layout_width="80dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@string/start_date_text"
            android:textColor="@color/black"/>
        <Button
            android:id="@+id/applyChangeBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/gradient_one"
            android:textColor="@color/white"
            android:shadowColor="#585757"
            android:layout_weight="1"
            android:text="@string/apply"/>
    </LinearLayout>

    <include
        android:id="@+id/content_route"
        layout="@layout/content_route"/>
    <include
        android:id="@+id/bottom_sheet_stop_detail"
        layout="@layout/bottomsheet_stopdetail" />

</android.support.design.widget.CoordinatorLayout>